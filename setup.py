from setuptools import setup, find_namespace_packages

setup(name='pyspark-xml-parsing',
      version='0.0.1',
      description=f'Derive new attributes from XML when you have XPATH transformations.Also check out how to handle '
                  f'multi line XML',
      url='',
      author='Vitthal Mirji',
      author_email='vitthalmirji@gmail.com',
      packages=find_namespace_packages(),
      zip_safe=False)
